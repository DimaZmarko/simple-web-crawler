root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "db/migrations", "docs"]
  exclude_regex = ["_test.go"]
  delay = 200
  stop_on_error = true

[log]
  time = true
