{
  "mcpServers": {
    "postgres": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "--network", "simple-web-crawler_default",
        "--env-file", ".env",
        "crystaldba/postgres-mcp",
        "--access-mode=restricted"
      ]
    }
  }
}
